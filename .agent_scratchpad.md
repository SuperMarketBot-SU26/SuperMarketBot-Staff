# Scratchpad - Realign Digital Map with Real-world Physical Supermarket Arena

## Current Goal
- Tinh chỉnh bản đồ Staff: Ẩn các đường line đường đi (path lines) để giao diện sạch sẽ, trực quan.
- Đồng bộ cơ sở dữ liệu Azure SQL: Thiết lập lại bảng `NAVIGATION_NODE` (Node 1..8), bảng `NAVIGATION_EDGE` (toàn bộ các cạnh kết nối đồ thị Dijkstra), và ánh xạ `SHELF.NodeID = 1..6`.

## Progress / Completed Steps
- [x] Đã hoàn thành phỏng vấn vị trí 6 kệ, thu ngân, dock, cửa và kích thước 3.0m x 3.0m.
- [x] Đã khảo sát cơ sở dữ liệu Azure SQL:
  - Bảng `NAVIGATION_NODE` hiện chỉ có 3 node cũ (14, 15, 16).
  - Bảng `SHELF` liên kết `NodeID` đến các node cũ này.
  - Bảng `SHELF_SCAN` có 31 bản ghi liên kết tới `NavigationNodeID` 14, 15, 16.
  - `NodeID` trong `NAVIGATION_NODE` là cột `IDENTITY(1,1)` nên cần dùng `SET IDENTITY_INSERT ON` khi ghi ID 1..8.
- [x] Đã thiết kế đồ thị `NAVIGATION_EDGE` kết nối 8 node (11 cạnh hai chiều).
- [x] Đã cập nhật `implementation_plan.md`.

## Key Decisions Made
- Bản đồ UI: Ẩn toàn bộ đường nét đứt `PATH_SEGMENTS` và chấm rẽ `NAV_NODES` theo yêu cầu người dùng, chỉ hiển thị kệ hàng, quầy thu ngân, dock sạc, cửa vào, thảm trung tâm và robot telemetry.
- Cơ sở dữ liệu: Tạo chính xác 8 Navigation Node (1..8), 11 Navigation Edges hai chiều, và cập nhật quan hệ FK tương ứng.

## Remaining TODOs
- [x] Thực thi script SQL tạo Node 1..8, 11 Edges, liên kết `SHELF.NodeID`, cập nhật `SHELF_SCAN`.
- [x] Cập nhật `storeLayout.ts` và `MapCanvas.tsx` (loại bỏ path lines, cập nhật tọa độ kệ, thêm thảm trung tâm).
- [ ] Chụp ảnh nghiệm thu trên điện thoại Android và xác nhận trực quan.

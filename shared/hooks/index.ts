/**
 * Cross-feature hooks. Each entry here is used by ≥2 features and is
 * too generic to live under any single one of them.
 */
export { apiErrorMessage, useApiErrorMessage } from "./useApiErrorMessage";